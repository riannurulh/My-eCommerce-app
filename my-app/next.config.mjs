/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['loremflickr.com','i.ibb.co.com'], // Add other domains here as needed
      },
};

export default nextConfig;
